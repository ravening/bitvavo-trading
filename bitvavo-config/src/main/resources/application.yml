---
server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ravening/bitvavo-config.git
          default-label: main
  #          clone-on-start: true
          username: <YOUR GITHUB USERNAME>
          password: <YOUR GITHUB PASSWORD>
  application:
    name: bitvavo-config
